changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo-mesh/issues/1564
    description: >
      Gloo Mesh will now discover an ExternalIP on an Ingress Gateway service
      if the status is not set. This value should be populated by
      the user when using a non-kubernetes LoadBalancer.