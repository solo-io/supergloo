# Code generated by skv2. DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  labels:
    app: service-mesh-hub
    app.kubernetes.io/name: service-mesh-hub
  name: settings.settings.smh.solo.io
spec:
  group: settings.smh.solo.io
  names:
    kind: Settings
    listKind: SettingsList
    plural: settings
    singular: settings
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        spec:
          description: Configure global settings and defaults.
          properties:
            mtls:
              description: Configure mTLS settings for TrafficTargets.
              properties:
                defaultMtls:
                  description: If true, by default enable mTLS for all TrafficTargets unless explicitly disabled by TrafficPolicies.
                  type: boolean
              type: object
          type: object
        status:
          properties:
            observedGeneration:
              description: |-
                The most recent generation observed in the the Settings metadata.
                If the observedGeneration does not match generation, the controller has not processed the most
                recent version of this resource.
              format: int64
              type: integer
          type: object
      type: object
  versions:
  - name: v1alpha2
    served: true
    storage: true
