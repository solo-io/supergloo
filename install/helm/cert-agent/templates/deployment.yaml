# Code generated by skv2. DO NOT EDIT.



{{- $certAgent := $.Values.certAgent}}
---

{{- define "cert-agent.deploymentSpec"}}

{{/* Define variables in function scope */}}
{{- $certAgent := $.Values.certAgent}}
{{- $certAgentImage := $certAgent.image }}
{{- $certAgentExtraPodLabels := $certAgent.extraPodLabels }}
{{- $certAgentExtraPodAnnotations := $certAgent.extraPodAnnotations }}
{{- $certAgentExtraDeploymentLabels := $certAgent.extraDeploymentLabels }}
{{- $certAgentExtraDeploymentAnnotations := $certAgent.extraDeploymentAnnotations }}

# Deployment manifest for cert-agent

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cert-agent
    {{- range $key, $value := $certAgentExtraDeploymentLabels }}
    {{ $key }}: {{ $value }}
    {{- end }}
  annotations:
    app.kubernetes.io/name: cert-agent
    {{- range $key, $value := $certAgentExtraDeploymentAnnotations }}
    {{ $key }}: {{ $value }}
    {{- end }}
  name: cert-agent
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: cert-agent
      {{- range $key, $value := $certAgentExtraPodLabels }}
      {{ $key }}: {{ $value }}
      {{- end }}
  template:
    metadata:
      labels:
        app: cert-agent
        {{- range $key, $value := $certAgentExtraPodLabels }}
        {{ $key }}: {{ $value }}
        {{- end }}
      annotations:
        app.kubernetes.io/name: cert-agent
        prometheus.io/path: /metrics
        prometheus.io/port: "9091"
        prometheus.io/scrape: "true"
        sidecar.istio.io/inject: "false"
        {{- range $key, $value := $certAgentExtraPodAnnotations }}
        {{ $key }}: {{ $value }}
        {{- end }}
    spec:
      serviceAccountName: cert-agent
      containers:
      - image: {{ $certAgentImage.registry }}/{{ $certAgentImage.repository }}:{{ $certAgentImage.tag }}
        args:
        - --metrics-port={{ $.Values.certAgent.ports.metrics | default $.Values.defaultMetricsPort }}
        - --verbose={{ $.Values.verbose }}
{{- if $certAgent.env }}
        env:
{{ toYaml $certAgent.env | indent 10 }}
{{- end }}
        imagePullPolicy: {{ $certAgentImage.pullPolicy }}
        name: cert-agent
{{- if $certAgent.resources }}
        resources:
{{ toYaml $certAgent.resources | indent 10}}
{{- else}}
        resources:
          requests:
            cpu: 500m
            memory: 256Mi
{{- end}}
{{- if $certAgent.securityContext }}
        securityContext:
{{ toYaml $certAgent.securityContext | indent 10 }}
{{- else }}
        securityContext:
          runAsNonRoot: true
          runAsUser: 10101
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
{{- end }}
      {{- if $certAgentImage.pullSecret }}
      imagePullSecrets:
        - name: {{ $certAgentImage.pullSecret }}
      {{- end}}

{{- end }} {{/* define "cert-agent.deploymentSpec" */}}

{{/* Render cert-agent deployment template with overrides from values*/}}
{{- $certAgentDeploymentOverrides := dict }}
{{- if $certAgent.deploymentOverrides }}
{{- $certAgentDeploymentOverrides = $certAgent.deploymentOverrides  }}
{{- end }}
{{- include "skv2.utils.merge" (list . $certAgentDeploymentOverrides "cert-agent.deploymentSpec") }}

---

apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: cert-agent
  name: cert-agent
  namespace: {{ $.Release.Namespace }}


{{- define "cert-agent.serviceSpec"}}


# Service for cert-agent
---
# Service for cert-agent
{{/* Define variables in function scope */}}
{{- $certAgent := $.Values.certAgent}}
{{- $certAgentExtraServiceLabels := $certAgent.extraServiceLabels }}
{{- $certAgentExtraServiceAnnotations := $certAgent.extraServiceAnnotations }}

apiVersion: v1
kind: Service
metadata:
  labels:
    app: cert-agent
    {{- range $key, $value := $certAgentExtraServiceLabels }}
    {{ $key }}: {{ $value }}
    {{- end }}
  annotations:
    app.kubernetes.io/name: cert-agent
    {{- range $key, $value := $certAgentExtraServiceAnnotations }}
    {{ $key }}: {{ $value }}
    {{- end }}
  name: cert-agent
  namespace: {{ $.Release.Namespace }}
spec:
  selector:
    app: cert-agent
  type: {{ $certAgent.serviceType }}
  ports:
  - name: metrics
    port: {{ $certAgent.ports.metrics }}


{{- end }} {{/* define "cert-agent.serviceSpec" */}}

{{/* Render cert-agent service template with overrides from values*/}}
{{- $certAgentServiceOverrides := dict }}
{{- if $certAgent.serviceOverrides }}
{{- $certAgentServiceOverrides = $certAgent.serviceOverrides  }}
{{- end }}
{{- include "skv2.utils.merge" (list . $certAgentServiceOverrides "cert-agent.serviceSpec") }}


